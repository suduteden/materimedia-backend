[project]
name = "materimedia-backend"
version = "0.1.0"
description = "TTS Backend"
readme = "README.txt"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "edge-tts"
]

[tool.vercel]
entrypoint = "api/generate.py"
